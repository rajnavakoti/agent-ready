{
  "name": "context-gap-scanner",
  "version": "0.1.0",
  "description": "Paste your company docs. See where your AI agents will fail.",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "netlify-cli": "^18.0.0"
  }
}
