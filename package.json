{
  "name": "agent-ready-workspace",
  "private": true,
  "scripts": {
    "test": "pnpm --recursive test",
    "clean": "rm -rf node_modules packages/*/node_modules"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
